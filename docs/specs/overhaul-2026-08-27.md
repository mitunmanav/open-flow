# Spec: Overhaul 2026-08-27 — stabilize → ship

Status: approved plan. Date: 2026-08-27.
Links: [beat-wispr.md](beat-wispr.md) · [polish-v2.md](polish-v2.md) · [tasks/todo-overhaul.md](../../tasks/todo-overhaul.md)

## Objective

Land WIP polish, close honest Wispr gaps (no accounts/sync/Notetaker), tighten CI + docs freshness, then verify and ship.

Author Mitun only. No Co-Authored-By. Explicit `git add` paths. Push only on GO.

## Tech / verify baseline

```
./gradlew :app:testDebugUnitTest :app:lintDebug :app:assembleDebug
bash scripts/qa/gate.sh --quick
bash scripts/qa/visual-capture.sh
bash scripts/qa/functional-check.sh   # expect 6/6
```

Device: `of_win` Windows `-gpu host` + wrap-adb. Bubble idle 252x126.

## Phases (ordered)

### 0 — Stabilize ground

- Clean WIP into logical commits (docs / bubble / home+insights+setup / qa).
- Authorship gate: every new commit author Mitun; no Co-Authored-By trailers.
- **Accept:** `git status` clean (or only intentional untracked). `git log -5 --format=%B` has no Co-Authored-By.
- **Verify:** unit tests if env allows; commit source even if gradle env-fails (note blocker).

### 1 — Floating bubble P0

- `of_win` visual: idle pill looks solid; `BubbleVisualPainter` + `flow_bubble.xml` + geometry/service fix.
- Show-text / pulse prefs respected; no grey veil; dumpsys ~252x126.
- **Accept:** visual-capture 4 PNGs + layout; Geometry/Painter/prefs tests green.
- **Verify:** `functional-check.sh` bubble checks + `android layout` / screen on `of_win`.

### 2 — Home history at scale

- Paging `observeRecent(limit=200)` + Load more; debounce search 300ms via `HistorySearchPolicy`; sticky day headers; footer anti-clip.
- **Accept:** 1000+ rows no jank; search debounce unit-tested; Load more advances page.
- **Verify:** `HistorySearchPolicyTest` + manual Home scroll/search on device.

### 3 — Insights upgrade

- Aggregates in `InsightsAggregatePolicy` (WPM, cleaned delta, top app, peak hour, etc.).
- Wispr-beat UI: tiles + heatmap + by-app; share card via FileProvider.
- **Accept:** policy Truth tests; Insights screen shows aggregates; share produces file.
- **Verify:** `InsightsAggregatePolicyTest` + device Insights + share smoke.

### 4 — Setup polish

- 3-step FirstRun (A11Y → MIC → BATTERY → DONE); progress dots; OEM battery hint; TalkBack labels.
- **Accept:** wizard shows 3 dots + step cards; OEM hint per manufacturer; a11y contentDescription set.
- **Verify:** first-run path on fresh install / clear data; TalkBack spot-check.

### 5 — Repo organize + docs never stale

- Expand `DocsStaleScanTest`; README split clear **dev** vs **launch** paths.
- Mark finished todos under `tasks/done/`; keep offline-smart todo intact.
- **Accept:** DocsStaleScanTest fails on stale pointers; README launch vs build unambiguous.
- **Verify:** `./gradlew :app:testDebugUnitTest --tests DocsStaleScanTest`.

### 6 — Automation

- CI: authorship check, secret scan, play-check; gate + visual hooks; `tag → release.yml`.
- **Accept:** PR/push CI fails on bad author / leaked secret / play-check fail; tag cut triggers release workflow.
- **Verify:** dry-run scripts locally; inspect `.github/workflows`.

### 7 — Beat Wispr honest gaps

- Grow language catalog; spoken-emoji opt-in in cleanup; latency tune (partials / flush) — see [beat-wispr.md](beat-wispr.md).
- **NO** accounts, cloud sync, Notetaker/meetings.
- **Accept:** language + emoji + latency changes unit-tested; COMPARISON honesty preserved.
- **Verify:** golden cleanup corpus + language policy tests; no new account/sync code paths.

### 8 — Verify + ship

- Full: unit + lint + assemble; gate; visual-capture; functional 6/6.
- **Push only on explicit GO.**
- **Accept:** all green checklist; release notes + tag when asked.
- **Verify:** commands in Tech / verify baseline + `gate.sh --release` / play-check when cutting release.

## Out of scope

- Keyboard IME product.
- Accounts, ads, analytics, cloud history sync.
- Calling this a Wispr clone.

## Done when

Phase 0–8 checkboxes in `tasks/todo-overhaul.md` checked; Phase 8 verify green; Mitun says GO to push.

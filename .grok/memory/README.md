# Open Flow memory (tiny)

Three files. That is the system.

| File | Role |
|------|------|
| `../NOW.md` | Live WHERE / NEXT only. Rewrite each pickup. |
| `FACTS.md` | Durable truth. Append. Do not delete history. |
| `LESSONS.md` | Mistakes. Do not repeat. Append. |

Also: native Grok memory (`memory_search`). Same project.

**Rules**
- NOW stays short.
- FACTS = locked product / device / process.
- LESSONS = scars only.
- Search here before asking Mitun again.

package app.openflow.ui.privacy

/** Honest launch copy. Brain enhance can POST this utterance — never claim it never leaves. */
object PrivacyHonesty {
    const val HOME_FOOTER =
        "System STT may send audio. A brain you pick can POST this utterance. History stays on this phone unless you share it."

    const val SETTINGS_BODY =
        "History stays on this phone. Cloud ear is off in 0.1.5. A cloud brain you pick can POST this utterance."

    const val KEEP_FOREVER =
        "Store history in on-device SQLite (not encrypted). A cloud brain you pick can still POST this utterance."

    const val WALKTHROUGH =
        "Phone speech may use Google. A brain you pick can POST this utterance. History stays on this phone. You can wipe or never save. Open Flow has no server."

    const val SETUP_MIC =
        "Phone STT may send audio. Open Flow has no server. A brain you pick can POST this utterance."
}

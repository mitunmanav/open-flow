# F1 Android Scaffold Implementation Plan

> Superpowers: worktree `feat/01-scaffold`, TDD where pure, security defaults.

**Goal:** Buildable Android app shell; Mitun can `assembleDebug` and install APK.

**Architecture:** Single-module `app`, Compose MainActivity placeholder, security defaults (no INTERNET, cleartext off, backup off). Existing WIP files under root are brought onto the feature branch and completed.

**Tech Stack:** AGP 8.7.3, Gradle 8.9, Kotlin 2.0.21, Compose BOM 2024.10, minSdk 26, targetSdk 35

**Security:**
- No INTERNET permission
- network_security_config cleartext false
- allowBackup false
- RECORD_AUDIO declared but runtime later

---

### Task 1: Worktree

- [ ] `git worktree add .worktrees/01-scaffold -b feat/01-scaffold`
- [ ] Complete MainActivity, Application, theme, gradle wrapper
- [ ] Unit tests for pure modules still pass
- [ ] `./gradlew :app:assembleDebug`
- [ ] Commit `feat: Android Compose scaffold with security defaults`

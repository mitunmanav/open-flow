# F19 — STT reliability + modern UI

## STT (Android docs)

- Package visibility: `<queries>` for `RecognitionService` (API 30+)
- `SpeechRecognizer` may stream to remote; do not force offline-only when pack missing
- Prefer on-device when available; fall back to default recognizer on error
- ERROR_LANGUAGE_UNAVAILABLE / CLIENT → retry without EXTRA_PREFER_OFFLINE
- Clearer bubble error labels

## UI

- Sleeker M3 palette + larger radius
- Home: status hero, setup CTAs, last result, test field, less clutter
- Bottom nav: Home · History · Settings (primary path)
- Settings holds dict/snips/style/cleanup

## Deliver

Debug APK → Windows Desktop `grok/`

# F20 — Wispr Android gap fill (local)

> TDD. Caveman. DID / PASS-FAIL / NEXT. Worktree `feat/20-wispr-gaps`.
> **Not:** F19 STT extras, tiny model, other languages, cloud.

**Goal:** Ship the missing **local** Wispr Android holes from the 2026-08-12 audit.

**Architecture:** Pure policy objects + tests first. Service / Home / Notifier only apply numbers.

## Files

| Create | Role |
|--------|------|
| `bubble/IdleShrink.kt` + test | compact after 5s idle |
| `bubble/CopyChip.kt` + test | show 10s after commit |
| `bubble/SessionGuard.kt` + test | warn 270s / stop 300s |
| `bubble/WaveformBars.kt` + test | RMS → bars |
| `bubble/AppStylePolicy.kt` + test | pkg → personal/work/email |
| Modify `FlowAccessibilityService.kt` | timers, screen-on, waveform, copy, style |
| Modify `notify/DictationNotifier.kt` | Copy last action |
| Modify `ui/MainActivity.kt` Home setup | battery chip |
| Modify `text` polish entry | apply AppStylePolicy when pkg set |

## TDD (order)

1. RED IdleShrink.shouldCompact(idleMs, listening, dragging)
2. RED CopyChip.shouldShow(ageMs, listening)
3. RED SessionGuard.phase(elapsedMs) NONE/WARN/STOP
4. RED WaveformBars.fromRms(0f..10f) length 4, more filled when louder
5. RED AppStylePolicy.category("com.whatsapp")=personal, gmail=email, slack=work
6. GREEN implement
7. Wire service + notif + battery intent
8. `./gradlew :app:testDebugUnitTest :app:assembleDebug`

## Security

No new INTERNET. POST_NOTIFICATIONS already used. Battery settings is a user Intent. No scrape.

## Device

- Idle 5s → smaller orb
- Stop listen → Copy chip; tap copies last clean
- Listen 5+ min warn (inject clock in unit test; device: label shows time)
- Listen bars move with voice, not clock
- Home: Battery chip opens OEM/battery screen

# F0 Bootstrap Implementation Plan

> **For agentic workers:** Superpowers TDD + worktree + one commit.

**Goal:** Initialize git repo with process, license, security, gitignore. No app logic changes required beyond cleanup of broken incomplete code.

**Architecture:** Main branch holds process docs. Features land via `.worktrees/feat-*` branches, one commit each.

**Tech Stack:** git, markdown

---

### Task 1: Process files

**Files:**
- Create: `docs/PROCESS.md`, `SECURITY.md`, `LICENSE`, `README.md`, `.gitignore`
- Create: this plan

- [x] Write process + security docs
- [ ] `git init`, first commit on main: `chore: bootstrap open-flow process and security`
- [ ] Verify `.worktrees` is ignored

### Task 2: Clean broken incomplete data layer

**Files:**
- Fix or remove broken `SessionRepository` until F4 TDD worktree

- [ ] Ensure no non-compiling stubs block later scaffold

# Audit baseline — 2026-09-03

Repo state today. Reference for measuring future progress.

## Axes (5, 0-3 each, 15 total)

| Axis | Score | Why now |
|------|-------|---------|
| Privacy & FOSS purity | 2 | Zero analytics deps; allowBackup=false + dataExtractionRules exclude all; F-Droid metadata still absent |
| Reliability & honesty | 1 | H1/H2/H3/H5/H7/H8 fixes landing in uncommitted; chip-to-code coverage partial |
| Performance & battery | 1 | M3-A tee in flight; bubble 252x126 idle; no APK/cold-start numbers measured |
| Cost & portability | 1 | No billing code; Play listing scaffolded; F-Droid + reproducible build absent |
| Dictation quality (core) | 2 | N-best pick + golden corpus live; P2 cleanup stages landed; T10 homophone guard deferred; latency unmeasured in CI |

**Total: 7 / 15**

## What blocks a higher score today

### Privacy & FOSS (toward 3)
- No `fastlane/metadata/android/en-US/` mirror under repo root
- No reproducible-build script
- LICENSE recognized SPDX

### Reliability & honesty (toward 3)
- Some chip states still asserted off in tests where real code path exists (PostStopChips undo = canUndo wired but tests migrate next)
- Bubble TalkBack announcements still tied to "Open Flow" instead of action state
- Bold-action UI flows not audited end-to-end

### Performance & battery (toward 3)
- M3-A tee (`AppAudioCapture`+`WavFileConsumer`+`PcmResampler`) gated `M3TeeFlags.USE_TEE=false` until device matrix passes
- No idle-CPU / battery / memory benchmarks in `gate.sh`
- APK size + cold start not pinned in any test

### Cost & portability (toward 3)
- No F-Droid PR (no `fastlane/metadata/`)
- No reproducible-build.yml
- LibreTranslate / OSS-only cloud ear option not surfaced

### Dictation quality (toward 3)
- T10 HomophoneGuard deferred (golden homophone P already 1.000)
- Golden corpus P/R/F1 not run in CI on every PR
- Per-app style rules `text/StyleResolvePolicy.kt` exist but coverage not measured against real per-app dictations
- Whisper path hallucination guard: signature-phrase list kept, but ground-truth replay set does not yet pinloop-collapses in CI

## In-flight (already touching the tree)

These count toward the next baseline score, not the current one:

- M3-A tee (audio/ — gated off)
- H7 undo chip = canUndo
- H8 keystore fail-closed
- H5 battery seen-after-grant
- H1 cloud-fatal notice copy
- H2/H3 cloud stopAndFlush + retry-replays-WAV
- LaptopEar stub removed (Engine/EnginePrefs cleaned)
- RecieverExportPolicy wired (post-33 isolation)
- AGENTS.md trim (132 → 87 lines, in working tree)
- Architecture gates extracted (this batch)

## Verdict

Two axes (privacy, dictation) are honest 2s. Three axes (reliability, perf, portability) sit at 1 — closeable in the cleanup phase plus one short follow-up sprint. No axis at 0. Wispr Flow still wins on polished 0-edit rate and ecosystem reach; we win on offline, FOSS, no-tier, and reversible control.

## Re-measure cadence

- Re-baseline at start of every new phase (`P?` in `tasks/active/`)
- Pin golden corpus + smoke numbers in `gate.sh` before claiming axe up

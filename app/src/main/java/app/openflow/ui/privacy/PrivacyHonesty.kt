package app.openflow.ui.privacy

/** Honest launch copy. Brain enhance can POST this utterance — never claim it never leaves. */
object PrivacyHonesty {
    const val HOME_FOOTER =
        "System STT may send audio. A brain you pick can POST this utterance. History stays on this phone unless you share it."

    const val SETTINGS_BODY =
        "History stays on this phone. Cloud ear stays off until you pick one (Sarvam / etc). A cloud brain you pick can POST this utterance."

    const val KEEP_FOREVER =
        "Store history in on-device SQLite (not encrypted). A cloud brain you pick can still POST this utterance."

    const val INSIGHTS_VOICE =
        "Usage stays on this phone. Refresh Voice sends only counts and top words to the brain you pick — not full dictations."

    const val SETUP_MIC =
        "Phone speech may send audio. We have no server. A rewrite you pick can POST this line."

    const val WALKTHROUGH =
        "Phone speech may use Google. A rewrite you pick can POST this line. History stays here. Wipe anytime. No Open Flow server."
}

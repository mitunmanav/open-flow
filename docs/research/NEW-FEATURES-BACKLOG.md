# New features backlog (local, ranked)

Only items feasible **without** cloud LLM.

## P0 — product truth
1. A11y Bound gate before any "works" claim
2. No force-stop killing service during QA
3. Honest High = rules, not AI rewrite label
4. Spoken cmds complete map (punct, layout, backspace)

## P1 — feel closer to Wispr Android
5. Context-aware: read existing field text → append/splice correctly
6. Hold-to-dictate on bubble long-press
7. Clipboard + Paste fallback when insert fails
8. Session raw always saved; "show raw" in history
9. STT quality formatting mode toggle (latency vs quality)
10. Bubble size + opacity + auto-shrink polish

## P2 — power user local
11. Phrase map JSON editable (advanced)
12. Per-app style override (local prefs, not AI)
13. Snippet trigger expansion reliability
14. Dictionary import/export
15. Debug inject broadcast (already pattern) for QA

## P3 — later / hard
16. Tiny on-device model (optional APK flavor) — still a model
17. Wear / tile
18. Always-on wake (battery hell)
19. Multi-language packs

## Explicit non-goals (no AI)
- Command Mode “make this formal”
- Cloud High rewrite parity
- 100 languages

